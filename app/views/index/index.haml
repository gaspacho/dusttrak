.row
  .col-md-offset-4
    =link_to('Todos (con umbral)', url(:all), :class => 'btn btn-primary')
    =link_to('Agrupados', url(:grouped), :class => 'btn btn-info')
    =link_to('Sobre el umbral', url(:below), :class => 'btn btn-danger')

-# encoding: utf-8
=partial 'historical/search'

%table#list.table.table-striped.table-hover.table-condensed
  %thead
    %tr
      %th.header Id
      %th.header Fecha
      %th.header Valor
      %th.header Cero
      %th.header Escala
      %th.header Concentración

  %tbody
    -@historical.each do |h|
      %tr{:class => h.pasado? ? 'list-row danger' : 'list-row' }
        %td.list-column=h.grd_id
        %td.list-column=h.timestamp
        %td.list-column=h.value
        %td.list-column=h.zero
        %td.list-column=h.scale
        %td.list-column=h.concentracion
